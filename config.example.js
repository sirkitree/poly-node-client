const config = {};
module.exports = config;

/**
 * This file should be copied to config.js, edited to contain real values, and
 * only be readable by local users that should have the proper access.
 */

/**
 * Get a key from your Google developer console
 * https://developers.google.com/poly/develop/web
 */
config.API_KEY = 'superSecretStringFromGoogle';
